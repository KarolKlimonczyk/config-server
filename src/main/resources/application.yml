spring:
  application:
    name: JvmfyConfigServer

  cloud:
    config:
      server:
        git:
          uri: ${GIT_CONFIG_FILE_URL}
          username: ${GIT_LOGIN}
          password: ${GIT_PASSWORD}
          clone-on-start: true

server:
  port: 8012